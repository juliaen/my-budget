import styled from '@emotion/styled'

export const StyledDoughnutRow = styled.div((props) => ({
    display: 'flex',
    flexDirection: 'row',
}))

export const StyledDoughnutColumn = styled.div((props) => ({
    display: 'flex',
    flex: 1,
    flexDirection: 'column',
}))
